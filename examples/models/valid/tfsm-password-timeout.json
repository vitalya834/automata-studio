{
  "schemaVersion": "1.0",
  "id": "tfsm-password-timeout",
  "name": "PasswordTimeout",
  "description": "TFSM (timeouts profile): the password prompt returns to idle after 30 s of silence.",
  "modelKind": "tfsm",
  "semanticProfile": "deterministic.partial",
  "timingProfile": "timeouts",
  "timeUnit": "s",
  "inputAlphabet": {
    "symbols": [{ "id": "request" }, { "id": "password" }]
  },
  "outputAlphabet": {
    "symbols": [{ "id": "prompt" }, { "id": "granted" }]
  },
  "states": [
    { "id": "idle" },
    { "id": "waiting", "timeout": { "after": 30, "to": "idle" } },
    { "id": "authorized" }
  ],
  "transitions": [
    { "id": "t1", "from": "idle", "to": "waiting", "input": "request", "output": "prompt" },
    { "id": "t2", "from": "waiting", "to": "authorized", "input": "password", "output": "granted" }
  ],
  "initial": { "stateId": "idle" },
  "provenance": {
    "createdBy": "manual",
    "sourceFormat": "handwritten",
    "timestamp": "2026-07-31T12:00:00Z"
  }
}
