{
  "schemaVersion": "1.0",
  "id": "tfsm-timeout-and-linear-delay",
  "name": "TimeoutWithLinearDelay",
  "description": "TFSM (timeoutsAndOutputDelays profile): session expires after 60 s; the ack delay grows linearly as 1 + 0.5*t.",
  "modelKind": "tfsm",
  "semanticProfile": "deterministic.partial",
  "timingProfile": "timeoutsAndOutputDelays",
  "timeUnit": "s",
  "inputAlphabet": {
    "symbols": [{ "id": "open" }, { "id": "data" }]
  },
  "outputAlphabet": {
    "symbols": [{ "id": "ready" }, { "id": "ack" }]
  },
  "states": [
    { "id": "closed" },
    { "id": "session", "timeout": { "after": 60, "to": "closed" } }
  ],
  "transitions": [
    { "id": "t1", "from": "closed", "to": "session", "input": "open", "output": "ready" },
    {
      "id": "t2",
      "from": "session",
      "to": "session",
      "input": "data",
      "output": "ack",
      "outputDelay": { "kind": "linearFamily", "base": 1, "slope": 0.5 }
    }
  ],
  "initial": { "stateId": "closed" },
  "provenance": {
    "createdBy": "manual",
    "sourceFormat": "handwritten",
    "timestamp": "2026-07-31T12:00:00Z"
  }
}
